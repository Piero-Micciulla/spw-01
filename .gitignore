# Site-local generated output and dependencies (matches at any depth).
dist/
node_modules/
graft/.cache/
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
