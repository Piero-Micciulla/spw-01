{
  "name": "how-many-weekends-are-left",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "TZ=Europe/Madrid node --test && TZ=America/New_York node --test",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && node scripts/check-build.mjs"
  },
  "engines": { "node": ">=20" }
}
