import{access,readFile,readdir,stat}from'node:fs/promises';import{join}from'node:path';const required=['index.html','privacy/index.html','404.html','robots.txt','sitemap.xml','styles.css','runtime-config.js','js/app.js','js/catalog.js','js/recommender.js','js/integrations.js','assets/icon.svg','assets/social-card.svg','site.webmanifest'];await Promise.all(required.map(file=>access(join('dist',file))));const html=await readFile('dist/index.html','utf8');for(const[name,pattern]of[['title',/<title>[^<]+<\/title>/],['description',/<meta name="description" content="[^"]+"/],['canonical',/<link rel="canonical" href="(?:https:\/\/[^"}]+|\/)"/],['Open Graph',/property="og:title"/],['Twitter card',/name="twitter:card"/],['heading',/<h1[ >]/],['structured data',/application\/ld\+json/],['privacy',/href="\/privacy\/"/],['time control',/data-budget="90"/],['category control',/data-category="anything"/]])if(!pattern.test(html))throw new Error(`Missing ${name}`);for(const file of required.filter(file=>/\.(?:html|txt|xml|js)$/.test(file))){const content=await readFile(join('dist',file),'utf8');if(/{{(?:SITE_URL|CURRENT_YEAR)}}/.test(content))throw new Error(`Unresolved token in ${file}`);if(/localhost|127\.0\.0\.1|example\.com|\.invalid/.test(content))throw new Error(`Placeholder/local URL in ${file}`);if(/G-[A-Z0-9]{8,}|ca-pub-\d+|YOUR[_-](?:ID|KEY)/.test(content))throw new Error(`Unexpected tracking/advertising token in ${file}`)}if(/\.innerHTML\s*=|insertAdjacentHTML/.test(await readFile('dist/js/app.js','utf8')))throw new Error('App must render catalog strings with textContent');const config=JSON.parse(await readFile('site.config.json','utf8'));if(!config.siteUrl&&/(?:rel="canonical" href|property="og:(?:url|image)" content|name="twitter:image" content)="https:\/\//.test(html))throw new Error('Unconfigured build has absolute metadata URL');const social=await readFile('dist/assets/social-card.svg','utf8');if(!/width="1200" height="630"/.test(social))throw new Error('Social image must be 1200x630');async function total(dir){let bytes=0;for(const entry of await readdir(dir,{withFileTypes:true}))bytes+=entry.isDirectory()?await total(join(dir,entry.name)):(await stat(join(dir,entry.name))).size;return bytes}console.log(`Build checks passed. Dist size: ${(await total('dist')/1024).toFixed(1)} KiB.`);
