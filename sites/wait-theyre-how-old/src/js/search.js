export const normalize=value=>value.normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ');
const scoreOne=(value,q)=>{if(value===q)return 0;if(value.startsWith(q))return 10;if(value.split(' ').some(x=>x.startsWith(q)))return 20;if(value.includes(q))return 30;return Infinity};
export function searchPeople(people,query,limit=8){const q=normalize(query);if(!q)return[];return people.map((person,index)=>{const name=normalize(person.name),direct=scoreOne(name,q),alias=Math.min(...(person.aliases||[]).map(a=>scoreOne(normalize(a),q)),Infinity);return{person,index,score:Math.min(direct,alias+40)}}).filter(x=>Number.isFinite(x.score)).sort((a,b)=>a.score-b.score||a.person.name.localeCompare(b.person.name)).slice(0,limit).map(x=>x.person)}
export function pickSurprise(pool,currentId,random=Math.random){const choices=pool.filter(x=>x.id!==currentId);const source=choices.length?choices:pool;return source[Math.min(source.length-1,Math.floor(random()*source.length))]}
