const PREFIX='party-buy-list:v1:';export function load(key){try{const x=JSON.parse(localStorage.getItem(PREFIX+key));return{x:new Set(Array.isArray(x?.checked)?x.checked:[]),custom:Array.isArray(x?.custom)?x.custom.filter(i=>i&&typeof i.name==='string').slice(0,40):[]}}catch{return{x:new Set(),custom:[]}}}export function save(key,state){localStorage.setItem(PREFIX+key,JSON.stringify({checked:[...state.x],custom:state.custom}))}export function clear(key){localStorage.removeItem(PREFIX+key)}
