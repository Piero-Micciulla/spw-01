import{cp,mkdir,readFile,rm,writeFile}from'node:fs/promises';import{dirname,join}from'node:path';import{fileURLToPath}from'node:url';const root=join(dirname(fileURLToPath(import.meta.url)),'..'),source=join(root,'src'),output=join(root,'dist'),config=JSON.parse(await readFile(join(root,'site.config.json'),'utf8')),siteUrl=String(process.env.SITE_URL||config.siteUrl).replace(/\/$/,'');if(siteUrl&&!/^https:\/\/[a-z0-9.-]+(?::\d+)?$/i.test(siteUrl))throw Error('siteUrl must be an https origin');await rm(output,{recursive:true,force:true});await mkdir(output,{recursive:true});await cp(source,output,{recursive:true});for(const f of['index.html','sources/index.html','privacy/index.html','robots.txt','sitemap.xml']){const p=join(output,f);await writeFile(p,(await readFile(p,'utf8')).replaceAll('{{SITE_URL}}',siteUrl))}if(!siteUrl){await writeFile(join(output,'robots.txt'),'User-agent: *\nAllow: /\n');await writeFile(join(output,'sitemap.xml'),'<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"></urlset>')}console.log(`Built ${output}`)
