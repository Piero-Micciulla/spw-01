# Can I Plug This In?

SPW 016. A static, conservative travel electricity compatibility checker. It compares the exact rating entered by a user with published destination supply data and a confirmed plug type. No backend, runtime API, analytics, ads, cookies, or affiliate links.

Run `npm test`, `npm run check`, or `npm run dev` here, or use the repository root commands.
