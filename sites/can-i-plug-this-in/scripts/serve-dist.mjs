import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,normalize}from'node:path';import'./build.mjs';const root=join(process.cwd(),'dist'),port=Number(process.env.PORT||4196),types={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript','.svg':'image/svg+xml','.xml':'application/xml','.txt':'text/plain','.webmanifest':'application/manifest+json'};createServer(async(req,res)=>{try{let p=decodeURIComponent(new URL(req.url,'http://x').pathname);if(p.endsWith('/'))p+='index.html';let file=normalize(join(root,p));if(!file.startsWith(root))throw 0;try{if((await stat(file)).isDirectory())file=join(file,'index.html')}catch{}const body=await readFile(file);res.writeHead(200,{'content-type':types[extname(file)]||'application/octet-stream'});res.end(body)}catch{res.writeHead(404,{'content-type':'text/html'});res.end(await readFile(join(root,'404.html')))}}).listen(port,'127.0.0.1',()=>console.log(`http://127.0.0.1:${port}`));
