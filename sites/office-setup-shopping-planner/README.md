# Office Setup Shopping Planner

SPW 015. A static, local-first office procurement planner. It turns six compact choices into a categorized, costed and checkable materials schedule. No backend, runtime APIs, analytics, ads, or affiliate links.

Run `npm test`, `npm run check`, or `npm run dev` from this directory; root commands also discover the site automatically.
