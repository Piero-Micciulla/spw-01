import test from'node:test';import assert from'node:assert/strict';import{dayDifference,daysInMonth,isLeapYear,isValidDate,moveDay,parseDate,relativeCopy,weekdayName}from'../src/js/calendar.js';
test('known weekdays are deterministic',()=>{for(const[value,expected]of[['2000-01-01','Saturday'],['1969-07-20','Sunday'],['2024-02-29','Thursday'],['2031-04-17','Thursday'],['2026-09-21','Monday'],['1600-01-01','Saturday'],['9999-12-31','Friday']])assert.equal(weekdayName(parseDate(value)),expected)});
test('Gregorian leap year rules',()=>{assert.equal(isLeapYear(2000),true);assert.equal(isLeapYear(1900),false);assert.equal(isLeapYear(2100),false);assert.equal(isLeapYear(2400),true);assert.equal(daysInMonth(2000,2),29);assert.equal(daysInMonth(1900,2),28)});
test('strict validation rejects malformed and impossible dates',()=>{for(const value of['','2024-2-29','nope','2023-02-29','2024-04-31','2024-00-01','2024-13-01','1599-12-31','10000-01-01'])assert.equal(parseDate(value),null);assert.equal(isValidDate(),false);assert.deepEqual(parseDate('2031-04-17'),{year:2031,month:4,day:17})});
test('navigation crosses leap, month, year, and supported boundaries',()=>{assert.deepEqual(moveDay(parseDate('2024-02-28'),1),parseDate('2024-02-29'));assert.deepEqual(moveDay(parseDate('2024-02-29'),1),parseDate('2024-03-01'));assert.deepEqual(moveDay(parseDate('2023-02-28'),1),parseDate('2023-03-01'));assert.deepEqual(moveDay(parseDate('2000-03-01'),-1),parseDate('2000-02-29'));assert.deepEqual(moveDay(parseDate('2025-12-31'),1),parseDate('2026-01-01'));assert.deepEqual(moveDay(parseDate('2026-01-01'),-1),parseDate('2025-12-31'));assert.equal(moveDay(parseDate('1600-01-01'),-1),null);assert.equal(moveDay(parseDate('9999-12-31'),1),null)});
test('calendar-day differences and copy ignore DST duration',()=>{const today=parseDate('2024-03-10');assert.equal(dayDifference(parseDate('2024-03-11'),today),1);assert.equal(relativeCopy(today,today),'Today');assert.equal(relativeCopy(parseDate('2024-03-09'),today),'1 day ago');assert.equal(relativeCopy(parseDate('2024-03-12'),today),'2 days from today')});
